name: GitHub MCP Server Workflow — SYSTEM FREEZE

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  system-freeze:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse execution during pristine audit gate
        run: |
          echo 'SYSTEM FREEZE — NOT READY TO RESTART.'
          echo 'This historical workflow ref is suspended until the pristine restart gate is completed and Kirstie explicitly authorises restart.'
          exit 1

# Historical branch retained for provenance; ordinary CI, issue automation, publishing, release and registry automation are suspended.
