name: GitHub MCP Server Workflow — SYSTEM FREEZE

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  system-freeze:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse execution during pristine audit gate
        shell: bash
        run: |
          echo 'SYSTEM FREEZE — NOT READY TO RESTART.'
          echo 'This github-mcp-server workflow is suspended until the whole-system audit and pristine restart gate are complete and Kirstie explicitly authorises restart.'
          exit 1

# GLOBAL SYSTEM FREEZE — 2 SEPTEMBER 2026
# Original workflow is preserved in Git history for audit/salvage.
# No CI, issue automation, moderation, publishing, release or registry automation is permitted during the freeze.
